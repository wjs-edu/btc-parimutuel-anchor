.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn

# local debug artifacts
*.bak
scripts/
Cargo.lock

# local backups
.bak/
*.bak_*

# local backups (never commit)
programs/btc_parimutuel/src/*.bak_*

# allow repo scripts (needed for CI test manifest + audit)
!scripts/
!scripts/**

# local-only scripts artifacts
scripts/__pycache__/
scripts/gen_test_min.py
scripts/green-devnet.sh

# crank artifacts
/evidence/

# Evidence bundles / local artifacts
evidence_*.tgz
evidence_*.tgz.sha256

# Handoff packets (local)
handoff_packet_vfinal/

# Scratch / backups
tmp_scratch/

# Local helper scripts (keep out of git unless explicitly promoted)
scripts/deploy-devnet.sh
scripts/fund-demo-wallets.sh
scripts/fund_demo_wallets.js
scripts/preflight-funding.sh

# generated artifacts (PASS)
/artifacts/
/artifacts/**
